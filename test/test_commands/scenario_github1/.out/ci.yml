# DO NOT EDIT
# This is a compiled file, compiled with bash2yaml
# Recompile instead of editing this file.
#
# Compiled with the command: 
#     bash2yaml compile --in C:\github\bash2yaml\test\test_commands\scenario_github1\uncompiled --out C:\github\bash2yaml\test\test_commands\scenario_github1\.out

name: CI Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CI_TOOL: bash2yaml
  VERSION: 1.0.0
  NODE_ENV: production
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      BUILD_TARGET: dist
      BUILD_TYPE: release
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: |-
          # >>> BEGIN inline: scripts/install.sh
          echo "Installing dependencies..."
          npm ci
          echo "Dependencies installed."
          # <<< END inline
      - name: Build project
        run: |-
          # >>> BEGIN inline: scripts/build.sh
          echo "Building project..."
          npm run build
          echo "Build complete."
          # <<< END inline
        env:
          OPTIMIZATION: high

  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4
      - name: Run tests
        run: |-
          # >>> BEGIN inline: scripts/test.sh
          echo "Running tests..."
          npm test
          echo "Tests complete."
          # <<< END inline