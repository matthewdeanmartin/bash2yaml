# DO NOT EDIT
# This is a compiled file, compiled with bash2yaml
# Recompile instead of editing this file.
#
# Compiled with the command: 
#     bash2yaml compile --in src --out templates

spec:
  inputs:
    stage:
      default: test
      description: "Stage for the scan job."
    job-prefix:
      type: string
      default: "secret"
      description: "Prefix for the generated job name."
---
"$[[ inputs.job-prefix ]]-scan":
  stage: $[[ inputs.stage ]]
  script:
  - |-
    # >>> BEGIN inline: scripts/scan.sh
    set -euo pipefail

    echo "Scanning during the $[[ inputs.stage ]] stage"
    echo "scan complete"
    # <<< END inline